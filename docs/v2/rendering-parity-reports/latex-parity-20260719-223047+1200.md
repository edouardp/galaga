<!-- rumdl-disable MD013 MD033 -->

# Galaga v1/v2 LaTeX Rendering Parity Report

Generated: `2026-07-19T22:30:47+12:00`
Repository state: `galaga_v2@96a1bdf (dirty)`
Primary policy: legacy `Multivector.display()` versus facade `DisplayPolicy("full")`.

## Summary

| Result | Count |
|---|---:|
| Expressions audited | 73 |
| Succeeded | 65 |
| Different or errored | 8 |

A success means expression, concrete value, full teaching display, rich
wrapper, and compatible numeric coefficients all agreed exactly.

## How to review

For each difference, compare the rendered forms and check one reviewer
decision. Review the emitted source only when exact LaTeX commands matter.
Add channel-specific exceptions or a preferred third form in the notes.
Save the annotated report; its stable case IDs can then be promoted into
the executable difference ledger and implementation backlog.

## Successful expressions

- `default-cl3/add` — a + b — **expression succeeded**
- `default-cl3/subtract` — a - b — **expression succeeded**
- `default-cl3/negate-sum` — -(a + b) — **expression succeeded**
- `default-cl3/scalar-multiply` — 1.23456789 a — **expression succeeded**
- `default-cl3/scalar-divide-sum` — (a + b) / 3 — **expression succeeded**
- `default-cl3/power-sum` — (a + b)^2 — **expression succeeded**
- `default-cl3/geometric-product` — a b — **expression succeeded**
- `default-cl3/geometric-product-left-sum` — (a + b) b — **expression succeeded**
- `default-cl3/geometric-product-right-sum` — a (a + b) — **expression succeeded**
- `default-cl3/outer-product` — a wedge b — **expression succeeded**
- `default-cl3/outer-product-right-sum` — a wedge (a + b) — **expression succeeded**
- `default-cl3/left-contraction` — a left-contract (b wedge c) — **expression succeeded**
- `default-cl3/right-contraction` — (a wedge b) right-contract b — **expression succeeded**
- `default-cl3/hestenes-inner` — (a + b) Hestenes-inner (b wedge c) — **expression succeeded**
- `default-cl3/doran-lasenby-inner` — (a + b) Doran-Lasenby-inner (b wedge c) — **expression succeeded**
- `default-cl3/scalar-product` — (a wedge b) scalar-product itself — **expression succeeded**
- `default-cl3/metric-inner-product` — (a wedge b) metric-inner-product itself — **expression succeeded**
- `default-cl3/antidot-product` — antidot_product(a, b) — **expression succeeded**
- `default-cl3/commutator` — commutator(a, b) — **expression succeeded**
- `default-cl3/anticommutator` — anticommutator(a, b) — **expression succeeded**
- `default-cl3/geometric-antiproduct` — geometric_antiproduct(a, b) — **expression succeeded**
- `default-cl3/regressive-product` — (a wedge b) vee (b wedge c) — **expression succeeded**
- `default-cl3/left-interior-product` — left_interior_product(a, b wedge c) — **expression succeeded**
- `default-cl3/right-interior-product` — right_interior_product(a wedge b, b) — **expression succeeded**
- `default-cl3/transwedge` — transwedge(a, b, 0) — **expression succeeded**
- `default-cl3/transwedge-antiproduct` — transwedge_antiproduct(a, b, 0) — **expression succeeded**
- `default-cl3/reverse-sum` — reverse(a + b) — **expression succeeded**
- `default-cl3/grade-involution` — grade_involution(a + b) — **expression succeeded**
- `default-cl3/conjugate` — conjugate(a + b) — **expression succeeded**
- `default-cl3/dual` — dual(a wedge b) — **expression succeeded**
- `default-cl3/undual` — undual(dual(a wedge b)) — **expression succeeded**
- `default-cl3/complement` — complement(a wedge b) — **expression succeeded**
- `default-cl3/uncomplement` — uncomplement(complement(a wedge b)) — **expression succeeded**
- `default-cl3/antireverse` — antireverse(a wedge b) — **expression succeeded**
- `default-cl3/metric-apply` — metric_apply(a + b) — **expression succeeded**
- `default-cl3/antimetric-apply` — antimetric_apply(a + b) — **expression succeeded**
- `default-cl3/bulk-part` — bulk_part(1 + a + a wedge b) — **expression succeeded**
- `default-cl3/weight-part` — weight_part(1 + a + a wedge b) — **expression succeeded**
- `default-cl3/right-hodge-dual` — right_hodge_dual(a wedge b) — **expression succeeded**
- `default-cl3/left-hodge-dual` — left_hodge_dual(a wedge b) — **expression succeeded**
- `default-cl3/right-weight-dual` — right_weight_dual(a wedge b) — **expression succeeded**
- `default-cl3/left-weight-dual` — left_weight_dual(a wedge b) — **expression succeeded**
- `default-cl3/unit` — unit(a + b) — **expression succeeded**
- `default-cl3/inverse` — inverse(2 + 0.25 a) — **expression succeeded**
- `default-cl3/even-grades` — even_grades(1 + a + a wedge b) — **expression succeeded**
- `default-cl3/odd-grades` — odd_grades(1 + a + a wedge b) — **expression succeeded**
- `default-cl3/exp` — exp(0.25 a wedge b) — **expression succeeded**
- `default-cl3/log` — log(exp(0.25 a wedge b)) — **expression succeeded**
- `default-cl3/grade-projection` — grade((a + b) c, 1) — **expression succeeded**
- `default-cl3/squared` — squared(a + b) — **expression succeeded**
- `default-cl3/sqrt` — sqrt(exp(0.25 a wedge b)) — **expression succeeded**
- `default-cl3/norm2` — norm2(a + b) — **expression succeeded**
- `default-cl3/sandwich` — sandwich(exp(0.25 a wedge b), c) — **expression succeeded**
- `default-cl3/metric-regressive-product` — metric_regressive_product(a wedge b, b wedge c) — **expression succeeded**
- `default-cl3/right-complement-alias` — right_complement(a wedge b) — **expression succeeded**
- `default-cl3/left-complement-alias` — left_complement(a wedge b) — **expression succeeded**
- `default-cl3/named-literal-deduplication` — u := 2 e1 + e2 — **expression succeeded**
- `default-cl3/anonymous-expression-value` — (a + b) c, anonymous full display — **expression succeeded**
- `default-cl3/near-zero-value` — x := 1 + 1.4524e-16 e1 — **expression succeeded**
- `default-cl3/six-significant-digits` — x := 1.23456789 e1 — **expression succeeded**
- `lengyel-rga/rga-geometric-product` — RGA geometric_product(a + b, b wedge c) — **expression succeeded**
- `lengyel-rga/rga-metric-inner-product` — RGA metric_inner_product(a wedge b, a wedge b) — **expression succeeded**
- `lengyel-rga/rga-antidot-product` — RGA antidot_product(a, b) — **expression succeeded**
- `lengyel-rga/rga-right-weight-dual` — RGA right_weight_dual(a wedge b) — **expression succeeded**
- `lengyel-rga/rga-transwedge` — RGA transwedge(a, b, 1) — **expression succeeded**

## Differences

### `default-cl3/lie-bracket`

Expression intent: **lie_bracket(a, b)**

#### Rendered Expression (v1 vs v2)

$$
x \quad = \quad \tfrac{1}{2}[a,\, b] \quad = \quad e_{12}

\\\\\

x \quad = \quad [a,\, b] \quad = \quad 2 e_{12}
$$

#### Emitted Latex (v1 vs v2)

```latex
x \quad = \quad \tfrac{1}{2}[a,\, b] \quad = \quad e_{12}

x \quad = \quad [a,\, b] \quad = \quad 2 e_{12}
```

#### Reviewer decision

- [x] Accept v2
- [ ] Match legacy v1
- [ ] Prefer another rendering
- [ ] Investigate as defect

Reviewer notes:

> Accepted Galaga 2 correction: lie_bracket is the unscaled commutator and uses the same bracket notation.

### `default-cl3/jordan-product`

Expression intent: **jordan_product(a, a)**

#### Rendered Expression (v1 vs v2)

$$
x \quad = \quad \tfrac{1}{2}\{a,\, a\} \quad = \quad 1

\\\\\

x \quad = \quad \{a,\, a\} \quad = \quad 2
$$

#### Emitted Latex (v1 vs v2)

```latex
x \quad = \quad \tfrac{1}{2}\{a,\, a\} \quad = \quad 1

x \quad = \quad \{a,\, a\} \quad = \quad 2
```

#### Reviewer decision

- [x] Accept v2
- [ ] Match legacy v1
- [ ] Prefer another rendering
- [ ] Investigate as defect

Reviewer notes:

> Accepted Galaga 2 correction: jordan_product is the unscaled anticommutator and uses the same brace notation.

### `default-cl3/reverse-atom`

Expression intent: **reverse(a)**

#### Rendered Expression (v1 vs v2)

$$
x \quad = \quad \tilde{a} \quad = \quad e_{1}

\\\\\

x \quad = \quad \widetilde{a} \quad = \quad e_{1}
$$

#### Emitted Latex (v1 vs v2)

```latex
x \quad = \quad \tilde{a} \quad = \quad e_{1}

x \quad = \quad \widetilde{a} \quad = \quad e_{1}
```

#### Reviewer decision

- [x] Accept v2
- [ ] Match legacy v1
- [ ] Prefer another rendering
- [ ] Investigate as defect

Reviewer notes:

> Accepted Galaga 2 typography: use the consistently wide \widetilde accent.

### `default-cl3/outerexp`

Expression intent: **outerexp(a)**

#### Rendered Expression (v1 vs v2)

$$
\text{render failed: RecursionError}

\\\\\

x \quad = \quad \operatorname{outerexp}\left(a\right) \quad = \quad 1 + e_{1}
$$

#### Emitted Latex (v1 vs v2)

```latex
\text{render failed: RecursionError}

x \quad = \quad \operatorname{outerexp}\left(a\right) \quad = \quad 1 + e_{1}
```

#### Reviewer decision

- [x] Accept v2
- [ ] Match legacy v1
- [ ] Prefer another rendering
- [ ] Investigate as defect

Reviewer notes:

> Accepted Galaga 2 rendering; legacy v1 raises RecursionError.

### `default-cl3/outersin`

Expression intent: **outersin(a)**

#### Rendered Expression (v1 vs v2)

$$
\text{render failed: RecursionError}

\\\\\

x \quad = \quad \operatorname{outersin}\left(a\right) \quad = \quad e_{1}
$$

#### Emitted Latex (v1 vs v2)

```latex
\text{render failed: RecursionError}

x \quad = \quad \operatorname{outersin}\left(a\right) \quad = \quad e_{1}
```

#### Reviewer decision

- [x] Accept v2
- [ ] Match legacy v1
- [ ] Prefer another rendering
- [ ] Investigate as defect

Reviewer notes:

> Accepted Galaga 2 rendering; legacy v1 raises RecursionError.

### `default-cl3/outercos`

Expression intent: **outercos(a)**

#### Rendered Expression (v1 vs v2)

$$
\text{render failed: RecursionError}

\\\\\

x \quad = \quad \operatorname{outercos}\left(a\right) \quad = \quad 1
$$

#### Emitted Latex (v1 vs v2)

```latex
\text{render failed: RecursionError}

x \quad = \quad \operatorname{outercos}\left(a\right) \quad = \quad 1
```

#### Reviewer decision

- [x] Accept v2
- [ ] Match legacy v1
- [ ] Prefer another rendering
- [ ] Investigate as defect

Reviewer notes:

> Accepted Galaga 2 rendering; legacy v1 raises RecursionError.

### `default-cl3/outertan`

Expression intent: **outertan(a)**

#### Rendered Expression (v1 vs v2)

$$
\text{render failed: RecursionError}

\\\\\

x \quad = \quad \operatorname{outertan}\left(a\right) \quad = \quad e_{1}
$$

#### Emitted Latex (v1 vs v2)

```latex
\text{render failed: RecursionError}

x \quad = \quad \operatorname{outertan}\left(a\right) \quad = \quad e_{1}
```

#### Reviewer decision

- [x] Accept v2
- [ ] Match legacy v1
- [ ] Prefer another rendering
- [ ] Investigate as defect

Reviewer notes:

> Accepted Galaga 2 rendering; legacy v1 raises RecursionError.

### `default-cl3/grades`

Expression intent: **grades(1 + a + a wedge b, [0, 2])**

#### Rendered Expression (v1 vs v2)

$$
x \quad = \quad 1 + e_{12}

\\\\\

x \quad = \quad \langle 1 + a + a \wedge b \rangle_{[0,2]} \quad = \quad 1 + e_{12}
$$

#### Emitted Latex (v1 vs v2)

```latex
x \quad = \quad 1 + e_{12}

x \quad = \quad \langle 1 + a + a \wedge b \rangle_{[0,2]} \quad = \quad 1 + e_{12}
```

#### Reviewer decision

- [x] Accept v2
- [ ] Match legacy v1
- [ ] Prefer another rendering
- [ ] Investigate as defect

Reviewer notes:

> Accepted Galaga 2 improvement: preserve provenance and render a multi-grade projection as angle brackets subscripted by the selected grade list.

## Coverage

- Shared registered legacy/facade operations: 45
- Shared registered operations exercised: 45
- Total operation IDs exercised, including structural helpers: 60

Every shared registered expression operation has at least one case.

[tool.uv.workspace]
members = ["packages/galaga"]
exclude = ["packages/galaga_marimo"]

[dependency-groups]
dev = [
    "galaga",
    "marimo>=0.21.1",
    "matplotlib>=3.10.8",
    "pytest>=9.0.2",
    "pytest-cov>=7.1.0",
]

[tool.uv.sources]
galaga = { workspace = true }
